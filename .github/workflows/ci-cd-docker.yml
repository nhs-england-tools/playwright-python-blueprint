name: Full CI/CD + Docker

on:
  push:
    branches:
      - "**"
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  ci_cd:
    uses: ./.github/workflows/ci-cd-pull-request.yaml
    secrets: inherit

  build_docker:
    name: Build Docker Image
    needs: ci_cd
    if: ${{ success() }}
    uses: ./.github/workflows/build-docker-image.yaml
    secrets: inherit
